{
  "name": "python-ai-tutor",
  "version": "1.0.0",
  "description": "Python learning platform with AI tutor",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "dev": "cd frontend && python -m http.server 8000",
    "build": "echo 'No build required for static site'",
    "start": "echo 'Start frontend: cd frontend && python -m http.server 8000' && echo 'Start backend: cd backend && uvicorn main:app --host 0.0.0.0 --port 8001'"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}